import {Button, Input, PanelLayout, PromoItem} from "@/components";
import {useState} from "react";

export default function Orders() {
    const [positions, setPositions] = useState("")

    return (
        <PanelLayout title="Список заказов">


        </PanelLayout>
    )
}
